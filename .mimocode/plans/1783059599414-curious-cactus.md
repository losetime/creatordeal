# 修复：边框/分割线颜色改浅

## 问题
上一轮误将 `--color-border` 从 `#f1f5f9` 改为 `#e8edf4`（更深了），应该改浅。

## 方案
将边框颜色改为更接近背景色的浅色值，同时修复 header/侧边栏/卡片边框。

### 修改文件

**`app/globals.css`** — 亮色模式：
- `--color-border`: `#e8edf4` → `#f5f7fa`
- `--color-input`: `#e8edf4` → `#f5f7fa`
- `--color-sidebar-border`: `#e8edf4` → `#f5f7fa`

**`app/globals.css`** — 暗色模式：
- `--color-border`: `#3d5270` → `#2d3f55`
- `--color-input`: `#3d5270` → `#2d3f55`
- `--color-sidebar-border`: `#3d5270` → `#2d3f55`

**`app/(dashboard)/layout.tsx:31`** — header 分割线：
- 当前：`border-b`（无显式颜色）
- 改为：`border-b border-border`

**`components/ui/sidebar.tsx:365`** — 侧边栏分隔线：
- 已使用 `bg-sidebar-border`，跟随变量变化

**`components/ui/card.tsx:10`** — 卡片边框：
- 已使用 `border`（默认用 `--color-border`），跟随变量变化

**`components/ui/breadcrumb.tsx`** — 面包屑分隔符：
- 使用 `<ChevronRight />` 图标，无边框颜色需改

## 验证
- TypeScript 检查通过
- 生产构建通过
